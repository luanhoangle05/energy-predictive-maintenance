"""Build model-ready features from validated sensor data."""

import pandas as pd


def build_features(data: pd.DataFrame) -> pd.DataFrame:
    """Return a copy until feature definitions are added in a later milestone."""
    return data.copy()
